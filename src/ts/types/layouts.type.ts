export type navBarPropsType = {
  isAsideOpen: boolean;
  setIsAsideOpen: (isAsideOpen: boolean) => void;
};

export type theAsidePropsType = {
    isAsideOpen: boolean;
    setIsAsideOpen: (isAsideOpen: boolean) => void;
  };
  